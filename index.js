
/**
 * Expose `pie()`.
 */

module.exports = pie;

/**
 * Draw a pie on `ctx` with the given `percent` filled.
 *
 * @param {CanvasContext2d} ctx
 * @param {Number} percent
 * @api public
 */

function pie(ctx) {
  
}